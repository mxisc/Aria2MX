Generated aria2 runtime bundles are written here by `node scripts/prepare-aria2-runtime.mjs`.
By default the script prepares only the current `GOOS-GOARCH` target. Pass `all`
only when you intentionally want to prebuild every supported runtime archive.
